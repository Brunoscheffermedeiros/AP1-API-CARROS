info:
  name: 02 - Buscar por id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/carros/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
